<div class="page-header">
  <h1>Loan Requests</h1>
</div>

<div class="container">
  <div class="row">
    <% cache do %>
      <ul style="list-style: none">
      <% @categories.each do |category| %>
        <li class="category"><%= link_to category.title.capitalize, category_path(category) %></li>
      <% end %>
      </ul>
    <% end %>
  </div>

  <%= render "shared/search" %>

</div>
</div>

<br>

<%= render "shared/lender_thumbnail" %>
